stages:
  - test
  - post_status

unittest:
    stage: test
    script:
        - python test.py
    tags:
        - demo-runner
    cache:
        untracked: true

status:
    stage: post_status
    script:
        - python post_status.py
    tags:
        - demo-runner
    when: always
    cache:
        policy: pull