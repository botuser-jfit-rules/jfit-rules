stages:
  - test
  - post_status

unittest:
    stage: test
    script:
        - bash test.sh &> status.out
    tags:
        - demo-runner
    cache:
        paths:
            - status.out

status:
    stage: post_status
    script:
        - python post_status.py
    tags:
        - demo-runner
    when: always